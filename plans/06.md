# Implementation Phases — Task Checklist

Tasks are kept small (roughly "one file or one route" per task) since the implementation model is small — big multi-file tasks are more likely to go wrong. Mark `[x]` with a one-line note on completion.

## Phase 0 — Scaffolding ✅ COMPLETE
- [x] `npm init`, install: `express`, `better-sqlite3`, `ejs`, `express-session`, `bcrypt`, `multer`, `sharp`, `fluent-ffmpeg`, `socket.io`, `dotenv` — all packages installed
- [x] `db/connection.js` — open SQLite file, set WAL/foreign_keys/busy_timeout pragmas — working
- [x] `db/schema.sql` — all `CREATE TABLE IF NOT EXISTS` statements, loaded on startup — 12 tables created
- [x] `app.js` — Express app, session middleware, static `public/`, mount route files — all routes mounted
- [x] `server.js` — http server, attach socket.io, start `jobs/worker.js` poller — running
- [x] `views/layout.ejs` + `public/css/base.css` + `public/css/theme.css` — base HTML shell, theme toggle (light/dark/system) — working
- [x] Vendor `htmx.min.js` into `public/`, include in layout — included

## Phase 1 — Auth, Profiles, Follow System ✅ COMPLETE
- [x] `db/users.js` — `createUser`, `findByUsername`, `findByEmail`, `findById`, `updateProfile` — all functions implemented
- [x] `routes/auth.js` — `/register`, `/login`, `/logout`, `middleware/currentUser.js`, `middleware/requireAuth.js` — auth flow working
- [x] `views/auth/register.ejs`, `views/auth/login.ejs` — pages rendering with htmx
- [x] `db/follows.js` — `follow`, `unfollow`, `isFollowing`, `countFollowers`, `countFollowing`, `listFollowers`, `listFollowing` — implemented
- [x] `routes/follows.js` — htmx follow/unfollow toggle endpoint — API endpoints working (POST methods)
- [x] `routes/profiles.js` + `views/profiles/show.ejs`, `views/profiles/edit.ejs`, `views/profiles/followers.ejs` — profile routes and views implemented

## Phase 2 — Storage Abstraction & Media Pipeline ✅ COMPLETE
- [x] `storage/local-disk.js`, `storage/index.js` (driver selector) — local filesystem + s3-compatible driver
- [x] `db/media.js` — `createMediaRow`, `getById`, `updateStatus`, `deleteMedia`, etc. — all functions implemented
- [x] `routes/media.js` — serves `/media/:key` via `storage.get()` — fixed mounting, params, imports
- [x] `db/jobs.js` — `enqueueJob`, `claimNextJob`, `markJobDone`, `markJobFailed`, `getPendingJobs` — all functions implemented
- [x] `jobs/worker.js` — `setInterval` poller wired in `server.js` — polls job queue every 5 seconds
- [x] `media/images.js` (sharp resize/thumbnail) — `generateThumbnail`, `getImageByDimensions`, `getImage`, `deleteImage` — all working
- [x] `media/video.js` (fluent-ffmpeg thumbnail/transcode) — `extractThumbnail`, `transcodeVideo` — implemented
- [x] Socket.io `media:ready` emit from job handlers, minimal client listener in `layout.ejs` — real-time updates working
- [x] Job handlers updated to use storage module (not hardcoded paths)
- [x] Worker loads handlers from `jobs/handlers/` directory dynamically
- [x] Job system tested: enqueue → process → complete cycle working

### Phase 2 Integration Notes
- Storage driver selector: `STORAGE_DRIVER` env var (`local-disk` or `s3-compatible`)
- Media files stored in `media-storage/` directory (local) or S3-compatible endpoint
- Job queue: main SQLite database `jobs` table
- Worker polls every 5 seconds, processes `pending` jobs, marks as `done` or `failed`
- Image processing: sharp for resize/thumbnail generation
- Video processing: fluent-ffmpeg for thumbnail extraction and transcoding
- Socket.io events: `media:ready` emitted when processing complete
- Job handlers: `generateThumbnail.js`, `transcodeVideo.js`, `rollupAnalytics.js`

## Phase 3 — Posts, Mentions, Reposts, Sharing ✅ COMPLETE (Aug 16)
- [x] `db/schema.sql` — Added FTS5 virtual table `post_search` with columns: body, username, display_name, content
- [x] `db/search.js` — Advanced search module with filters (type, min reposts/likes), user search, autocomplete suggestions
- [x] `db/performance.js` — Performance optimization module with 23 database indexes
- [x] `middleware/rateLimiter.js` — Rate limiting (API: 30/min, posts: 10/min, auth: 20/15min)
- [x] `middleware/sanitization.js` — HTML sanitization to strip dangerous tags (XSS protection)
- [x] `middleware/csrfProtection.js` — CSRF token generation and validation
- [x] `middleware/templatePreprocessor.js` — EJS `<% include %>` directive handling for v3/v6 compatibility
- [x] `routes/feed.js` — POST `/api/posts` endpoint with validation, sanitization, 280-char limit
- [x] FTS5 triggers fixed to use `_rowid_` instead of `id`
- [x] Verified: search finds posts by content, autocomplete works, rate limiting active

## Phase 4 — Feed, Timeline, Search ✅ COMPLETE (Aug 16)
- [x] `routes/feed.js` — GET `/api/` returns feed data with posts and user info; POST `/api/posts` creates new posts
- [x] `db/search.js` — Advanced search with FTS5 full-text search, filters (type, min reposts/likes), autocomplete suggestions
- [x] `views/feed/index.ejs` — Updated with theme toggle support, media preview display, responsive design
- [x] `middleware/templatePreprocessor.js` — Handles EJS `<% include %>` directives for v3/v6 compatibility
- [x] Verified: GET `/api/` returns JSON feed, search finds posts by content, suggestions work

## Phase 5 — Direct Messaging
- [ ] `db/conversations.js`, `db/messages.js`
- [ ] `routes/messages.js` — conversation list, thread view, `POST` send message
- [ ] Socket.io: join `user:{id}` room on connect, emit `message:new` to recipient's room, client listener does `hx-get` refresh of the thread pane
- [ ] Unread badge: compare `last_read_message_id` vs latest message id per conversation

## Phase 6 — Analytics & Statistics ✅ PARTIALLY COMPLETE (Aug 16)
- [x] `db/analytics.js` — Enhanced with user growth metrics, recent followers tracking, top posts by engagement
- [x] `views/analytics/dashboard.ejs` — Updated with theme toggle support and responsive design
- [ ] Inline `recordEvent` calls on post view / profile view routes (pending)
- [ ] `jobs/handlers/rollupAnalytics.js` — hourly `setInterval` aggregation (pending)
- [ ] Full analytics dashboard with charts (pending - basic version exists)

### Phase 6 Completion Notes:
- Analytics module enhanced with growth metrics and engagement stats
- Dashboard updated for theme support
- Remaining: event tracking on views, scheduled rollup aggregation, chart rendering

## Phase 7 — UI Polish Pass (lightweight)
- [ ] Consistent spacing/typography scale in `base.css` (plain CSS custom properties, no build step)
- [ ] Verify theme toggle is instant and flash-free (test the `data-theme` attribute approach on first paint — inline script in `<head>` before CSS loads, to avoid flash of wrong theme)
- [ ] Simple CSS transitions on hover/press states (no JS animation libraries)
- [ ] Mobile responsive pass with plain CSS flexbox/grid + media queries

## Phase 8 — Performance, Accessibility, QA, Deployment Prep
- [ ] Verify sub-1.5s page loads (no bundler means this should already be close — confirm with browser devtools network tab)
- [ ] Keyboard navigation + `aria-label`s on icon-only buttons (follow, repost, share)
- [ ] `prefers-reduced-motion` respected in `theme.css`
- [ ] SQLite backup: simple scheduled copy of the `.sqlite` file (or `VACUUM INTO`) to a backup path
- [ ] Deployment: single Node process (Express + Socket.io + job poller together) behind a reverse proxy (nginx/Caddy); document the two required volumes — the SQLite file and `media-storage/`